# Screenshots

Drop product captures here to embed in the root [`README.md`](../../README.md):

- `landing.png` — marketing landing page
- `dashboard.png` — populated coach dashboard
- `review.png` — the review workspace (video + timeline + moments)
- `share.png` — the public athlete share page

Suggested capture flow: run the app (`docker compose up --build` or the local
dev setup), log in with `demo@rallylens.app` / `password123`, and capture at a
~1280px-wide viewport for crisp, consistent images.
